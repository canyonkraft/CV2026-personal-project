# Generative Dog Images — Python Dependencies
# Install with: pip install -r requirements.txt
#
# GPU strongly recommended (CUDA 11.8+). For CPU-only testing, remove torch index URL.
# Install PyTorch first: https://pytorch.org/get-started/locally/

# ── Core deep learning ────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── HuggingFace ecosystem ─────────────────────────────────────────────────────
diffusers>=0.27.0
transformers>=4.39.0
accelerate>=0.28.0
peft>=0.10.0          # LoRA via PEFT

# ── Image captioning ──────────────────────────────────────────────────────────
# BLIP is shipped inside transformers; no extra install needed

# ── Evaluation metrics ────────────────────────────────────────────────────────
torchmetrics[image]>=1.3.0   # FID
torchmetrics[multimodal]>=1.3.0  # CLIP score

# ── Data / utilities ──────────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0        # required by FID internally
requests>=2.31.0
tqdm>=4.66.0

# ── Optional: xformers for memory-efficient attention (GPU only) ──────────────
# xformers>=0.0.23
